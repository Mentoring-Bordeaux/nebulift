name: poc-template
description: Basic template for a GitHub repository with a single file
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
